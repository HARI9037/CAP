# utils/
Purpose:
Pure frontend utility functions.
Responsibilities:
- formatTimestamp — display-friendly time formatting
- truncateText — shorten long responses for preview
- parseConfirmationPayload — extract action details for display
Rules:
- No API calls
- No React imports
- Pure functions only